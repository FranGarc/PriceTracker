<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Declare the activity here so it is visible to test modules -->
        <!-- Note: We don't add the intent-filter here; that stays in the :app module -->
        <activity
            android:name="com.franciscogarciagarzon.pricetracker.MainActivity"
            android:exported="true"
            />
    </application>

</manifest>